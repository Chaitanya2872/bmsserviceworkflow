<!DOCTYPE html>
<html>
<head><title>Login</title></head>
<body>
  <h1>Welcome</h1>
  <% if (user) { %>
    <p>You are logged in as <%= user.email %></p>
    <img src="<%= user.profile_picture_url %>" width="100" />
    <br/><a href="/dashboard">Go to Dashboard</a><br/><a href="/logout">Logout</a>
  <% } else { %>
    <a href="/auth/google">Sign in with Google</a>
  <% } %>
</body>
</html>